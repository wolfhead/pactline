name: pactline-local

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: pactline
      POSTGRES_PASSWORD: pactline
      POSTGRES_DB: pactline
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pactline -d pactline"]
      interval: 2s
      timeout: 3s
      retries: 20
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - data

  rabbitmq:
    image: rabbitmq:4.1-alpine
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 3s
      timeout: 5s
      retries: 20
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    networks:
      - data

  api:
    build:
      context: .
      dockerfile: deploy/api.Dockerfile
    environment:
      ADDR: :8080
      APP_ENV: development
      AUTH_PROVIDER: development
      APP_BASE_URL: ${PACTLINE_APP_BASE_URL:-http://localhost:${PACTLINE_HTTP_PORT:-5173}}
      DATABASE_URL: postgres://pactline:pactline@postgres:5432/pactline?sslmode=disable
      LOG_LEVEL: ${PACTLINE_LOG_LEVEL:-info}
      RABBITMQ_URL: amqp://guest:guest@rabbitmq:5672/
      ATTACHMENT_STORAGE_PROVIDER: local
      ATTACHMENT_LOCAL_ROOT: /var/lib/pactline/attachments
      SESSION_SECRET: ZGV2ZWxvcG1lbnQtc2Vzc2lvbi1zZWNyZXQtMzJieXQ=
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/readyz"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 10s
    init: true
    read_only: true
    tmpfs:
      - /tmp
    volumes:
      - attachment_data:/var/lib/pactline/attachments
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    networks:
      - app
      - data

  web:
    build:
      context: .
      dockerfile: deploy/web.Dockerfile
      args:
        PACTLINE_IDENTITY_MODE: development
    ports:
      - "127.0.0.1:${PACTLINE_HTTP_PORT:-5173}:8080"
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/healthz"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 10s
    read_only: true
    tmpfs:
      - /config:uid=10001,gid=10001,mode=0700
      - /data:uid=10001,gid=10001,mode=0700
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    networks:
      - app

volumes:
  postgres_data:
  rabbitmq_data:
  attachment_data:

networks:
  app:
  data:
    internal: true
