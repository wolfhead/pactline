# URL Utilities

This small library validates redirect targets, summarizes samples, and parses
HTTP ports. Review it for correctness and security. Run `npm test` for the
documented checks. Do not modify the repository.
