{
	admin off
	auto_https off
	servers {
		protocols h1
	}
}

:8080 {
	encode zstd gzip

	@backend path /api/* /healthz /readyz
	handle @backend {
		reverse_proxy api:8080
	}

	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
